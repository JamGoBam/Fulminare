"""Ranked alert queue for the dashboard.

TODO(CLAUDE.md Block 5): combine imbalance + chargeback risk into a priority score,
return top-N alerts with enough context for the UI card to render standalone.
"""
from __future__ import annotations
